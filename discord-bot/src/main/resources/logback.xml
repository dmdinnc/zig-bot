<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define the LOG_LEVEL property with a default value -->
    <property name="LOG_LEVEL" value="${LOG_LEVEL:-INFO}" />
    
    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root logger configuration -->
    <root level="${LOG_LEVEL}">
        <appender-ref ref="CONSOLE" />
    </root>
    
    <!-- Specific logger configurations to reduce noise from external libraries -->
    <logger name="net.dv8tion.jda" level="WARN" />
    <logger name="org.apache.http" level="WARN" />
    <logger name="io.netty" level="WARN" />
    <logger name="org.eclipse.jetty" level="WARN" />
    
    <!-- Keep our application logs at the configured level -->
    <logger name="com.zigbot" level="${LOG_LEVEL}" />
</configuration>
